import React from 'react';
import { PaginatedResponse } from './types/PaginatedResponse';
import { Film } from './types/Film';

export const MovieDetail = (props: PaginatedResponse<Film>) => {

    return (
        <>
        
        <h1>Movie title:</h1>
        </>
    );
};
